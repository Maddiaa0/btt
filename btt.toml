# btt — branch tree testing (this repo dogfoods itself)

[project]
packs = ["rust"]

[check]
extra = "warn"
order = "warn"
# Every test-bearing file must have a .tree spec (dogfooding the ratchet).
uncovered = "error"
