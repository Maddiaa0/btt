import { describe, it } from "vitest"; // adjust for your runner (jest/bun have globals)

{% for ev in events -%}
{% if ev.kind == "open" -%}
{{ ev.indent }}describe("{{ ev.name | js_string }}", () => {
{% elif ev.kind == "close" -%}
{{ ev.indent }}});

{% elif ev.kind == "test" -%}
{{ ev.indent }}it("{{ ev.name | js_string }}", () => {
{{ ev.indent }}  // btt:todo — {{ ev.text | line_safe }}
{{ ev.indent }}});

{% endif -%}
{% endfor -%}
